{
  "name": "schema",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "serve -L -s -n -u -p 2000 spec",
    "build": "ts-node bin/generate/route.ts",
    "lint": "biome check bin",
    "lint:fix": "biome check bin --apply",
    "test": "echo 'no test yet'"
  },
  "devDependencies": {
    "@himenon/openapi-typescript-code-generator": "0.27.4",
    "serve": "14.2.1",
    "ts-node": "10.9.2"
  }
}
