{
  "name": "example-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@biomejs/biome": "1.6.3",
    "@types/node": "20.11.30",
    "turbo": "1.13.0",
    "typescript": "5.4.3"
  }
}
